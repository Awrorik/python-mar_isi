class Enemy:
    def __init__(self, name , health,damage,Weapon):
        self.name=name
        self.health=health
        self.damage=damage
        self.damage=10
        self.Weapon=Weapon

    def attack(self,Player):
            print(self.name, "атакує", Player.name)
            Player.health -= self.damage
            print("HP ворога:", Player.health)

    def info(self):
        print("ворог", self.name)
        print("HP", self.health)

class Weapon:
    def __init__ (self, name, damage):
        self.name=name
        self.damage=damage
        self.damage=10

    def info(self):
        print("Зброя", self.name)
        print("Шкода", self.damage)

class Player:
    def __init__(self, name,  Weapon, health , damage):
        self.health=health
        self.name=name
        self.damage=damage
        self.Weapon=Weapon
        self.health = 100

    def show_golden_knife(self):
         print(self.name, "використовує", self.Weapon.golden_knife.name)

    def attack(self, enemy):
        print(self.name, "атакує", enemy.name)
        enemy.health -= self.damage
        print("HP ворога:", enemy.health)

    def show_attack(self):
            print(self.name, "Policia атакує Mellstroy", self.Weapon.Pistolet.name)

    def info(self):
            print("імя", self.name)
class Items:
    def __init__(self,name,value):
        self.name=name
        self.value=value

class Inventory:
    def __init__(self,Weapons,Items):
        self.items=["Golden knife", "coca_cola","chocolate","gold"]
        self.Weapons=Weapons
        self.Items=Items
        if len(self.items) < 2:
          print("ищи вещи они тебе понадобяться")
        else:
          print("у тебя уже достаточно вещей все супер")

    def watch_Inventory(self):
     for item in self.items:
         if item not in Inventory:
             print("Надо посмотреть есть инвентарь")

    def add_item(self, item):
      if item not in self.items:
        self.items.append(item)
        print("добавлено:", item)
      else:
        print("эта вещь уже есть")

    def info(self):
     print("Инвентарь",self.items)

inv = Inventory("Golden knife",["coca_cola","chocolate","gold"])
inv.info()
coca_cola=Items("Cocacola",20)
gold=Items("Gold",100)
chocolate=Items("Chocolate", 15)
golden_knife = Weapon("Golden knife", 25)
player = Player("Mellstroy", golden_knife, 150, 25)
player.info()
enemy=Enemy("Policia", 100,15,"Pistolet")
golden_knife=Weapon("Golden knife",  25)
golden_knife.info()
pistolet=Weapon("Pistolet", 15)
pistolet.info()

while player.health > 0 and enemy.health > 0:
    player.attack(enemy)
    if enemy.health <= 0:
        print("победа", player.name)
        break

    enemy.attack(player)
    if player.health <= 0:
        print("Победа", enemy.name)
        break
